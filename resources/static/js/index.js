function provera() {

    /*$.ajax({
        type: "GET",
        url: "http://127.0.0.1:8080/index_provera",
        dataType: "json",
        success: function (data) {
            //alert("Uloga index: " + data['uloga']);
            if(data['ulogovan']) {
                if(data['uloga'] === "GLEDALAC") window.location.href = "gledalac_index.html";
                else if (data['uloga'] === "MENADZER") window.location.href = "menadzer_index.html";
                else if (data['uloga'] === "ADMIN") window.location.href = "admin_index.html";
            }
            },
        error: function (){
            //alert("Greška! (index.js)");
        }
    });*/
}